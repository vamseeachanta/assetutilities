# Plan: PB hardware and utility readiness (#42)

- Issue: https://github.com/vamseeachanta/assetutilities/issues/42
- Tier: T3 (onboarding checklist; recommend close)
- Repo: assetutilities (proposed: close)
- Status: plan-review

## Context

Issue #42 is an onboarding checklist for a contributor named "PB"
covering: clone seanation, install GitHub / AutoCAD / FreeCAD / Git /
Blender / earphones / Notepad++ / VSCode / miniconda. Most items are
already checked off in the body.

Remaining unchecked items:
- Daily commit cycle using git-bash (assigned VA).
- Blender hardware compatibility / remote access.
- Miniconda further work assignment.

None of these are assetutilities deliverables. This is a personal
onboarding checklist that has largely served its purpose.

**Re-routing recommendation:** close #42. The remaining items are
either captured in #19 (daily commit cycle) or are personal-hardware
follow-ups that don't belong in the public issue tracker.

In-scope here: triage + close. No code.

## Plan

1. **Comment on #42.** Map remaining unchecked items:
   - "Daily commit cycle using git-bash" → already in #19 scope.
   - "Blender hardware compatibility" → personal hardware; not an
     assetutilities issue.
   - "Miniconda further work" → either folded into #32 (env mgmt)
     or dropped.
2. **Close #42** with a comment linking to #19 and #32 for the
   handful of items that may still apply.

## Acceptance criteria

- [ ] Comment on #42 mapping remaining items.
- [ ] #42 closed with `wontfix` or `superseded`.

## Open questions

- Is "PB" still an active contributor whose onboarding state we need
  to track? If yes, ask the maintainer to convert to a private doc.
