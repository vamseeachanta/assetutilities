basename: file_management

default:
  log_level: DEBUG
  config:
    overwrite:
      output: True

file_management:
  flag: True
  input_directory: scenarios_file_folder
  output_directory: NULL

  filename:
    extension: [yml]
    pattern: NULL
    filters:
      contains: []
      not_contains: []
      starts_with: []  
      ends_with: []    
      regex: NULL #'^[a-z]{2}_[0-9]{4}_.*$'  # 2 letters + _ + 4 digits + _ + anything ( customize accordingly  )          
      min_size_kb: 33       
      max_size_kb: 40     
      match_logic: NULL  # (AND/OR)
