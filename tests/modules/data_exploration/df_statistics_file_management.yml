basename: data_exploration
type:
  df_statistics:
    flag: True
    df_array: True
    df: True

data:
  type: file_management
  groups: []

master_settings:
  groups:
    columns: { x: [Heading], y: [Cx] }

file_management:
  flag: True
  input_directory: csv
  output_directory: NULL

  filename:
    extension: [csv]
    pattern: NULL
    filters:
      contains: []
      not_contains: []

default:
  log_level: DEBUG
  config:
    overwrite:
      output: True
